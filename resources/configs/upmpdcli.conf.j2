# upmpdcli configuration — generated by LX06 Flash Tool
# Reference: https://github.com/medoc92/upmpdcli

upnpiface =
pidfile = /var/run/upmpdcli.pid
friendlyname = {{ device_name | default('LX06-Speaker') }}

# MPD connection
mpdhost = localhost
mpdport = 6600

# OpenHome renderer
ohproductroom = LX06 Room
radiolist = 
